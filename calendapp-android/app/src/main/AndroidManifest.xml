<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upc.eetac.dsa.dsaqp1415g2.calendapp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalendappMainActivity" />
        <activity android:name=".EventPrivateDetailActiviy" />
        <activity android:name=".EditEventPrivateActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".GroupsActivity"/>
        <activity android:name=".CreateEventActivity"/>
        <activity
            android:name=".api.Register"
            android:label="@string/title_activity_register" >
        </activity>
    </application>

</manifest>
