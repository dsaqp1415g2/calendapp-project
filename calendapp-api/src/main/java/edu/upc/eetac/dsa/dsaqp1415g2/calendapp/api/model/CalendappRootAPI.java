package edu.upc.eetac.dsa.dsaqp1415g2.calendapp.api.model;

import java.util.List;

import javax.ws.rs.core.Link;

import org.glassfish.jersey.linking.InjectLink;
import org.glassfish.jersey.linking.InjectLinks;
import org.glassfish.jersey.linking.InjectLink.Style;

public class CalendappRootAPI {
	/*
	 * @InjectLinks({
	 * 
	 * @InjectLink(resource = CalendappRootAPIResource.class, style =
	 * Style.ABSOLUTE, rel = "self bookmark home", title = "Beeter Root API"),
	 * 
	 * @InjectLink(resource = StingResource.class, style = Style.ABSOLUTE, rel =
	 * "collection", title = "Latest stings", type =
	 * MediaType.BEETER_API_STING_COLLECTION),
	 * 
	 * @InjectLink(resource = StingResource.class, style = Style.ABSOLUTE, rel =
	 * "create-sting", title = "Create new sting", type =
	 * MediaType.BEETER_API_STING)}) private List<Link> links;
	 * 
	 * public List<Link> getLinks() { return links; }
	 * 
	 * public void setLinks(List<Link> links) { this.links = links; }
	 */
}
